{
    "buildCommand": "pnpm run build",
    "installCommand": "pnpm install",
    "outputDirectory": "./dist",
    "framework": "vite",
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/index.go"
        }
    ]
}